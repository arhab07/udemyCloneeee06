import { colors } from "./color";
import { space, lineHeights } from "./space";
import { sizes } from "./size";
import { fonts, fontWeights, fontSizes } from "./font";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};